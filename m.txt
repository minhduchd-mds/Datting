fix(admin): thêm nhãn cho mã lý do 6 và đưa admin vào typecheck gốc

Hai lỗi, cái thứ hai mới là gốc.

1. Migration 0002 thêm mã lý do 6 (lừa đảo) vào ReportReason, nhưng
   apps/admin/src/labels.ts giữ hai Record<ReportReason, ...> đầy đủ khoá
   (REASON_LABEL, REASON_TONE). Thiếu khoá 6 là không biên dịch được. Đây
   chính là cơ chế mà migration 0002 mô tả: thêm mã mới mà quên trọng số thì
   TypeScript chặn lại — nó hoạt động đúng như thiết kế.

2. `npm run typecheck` ở root KHÔNG bao gồm @datting/admin, nên bản kiểm
   local xanh trong khi CI đỏ. Đã thêm admin vào chuỗi. Nếu không sửa chỗ
   này thì lần sau vẫn lọt y hệt — đó mới là lỗi thật, dòng nhãn chỉ là
   triệu chứng.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
