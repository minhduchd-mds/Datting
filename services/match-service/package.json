{
  "name": "@datting/match-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "dev": "npm run build && node dist/src/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/pg": "^8.21.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "ioredis": "^6.0.0",
    "pg": "^8.23.0"
  }
}
